# LLM CLI Assistant

Простое CLI-приложение на Python для отправки запросов в LLM через API и получения ответов в консоли.

Проект создан как учебный пример работы с AI API.


---

## 📁 Структура проекта

```
llm-cli-app/
│
├── app/
│   ├── __init__.py
│   ├── main.py
│   └── llm_client.py
│
├── .env.example
├── requirements.txt
├── .gitignore
└── README.md
```

---

## ⚙️ Установка

### 1. Клонировать репозиторий

```bash
git clone https://github.com/AlekseyRodionov/llm-cli-app.git
cd llm-cli-app
```

### 2. Создать виртуальное окружение

Mac / Linux:

```bash
python -m venv venv
source venv/bin/activate
```

Windows:

```bash
python -m venv venv
venv\Scripts\activate
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

---

## 🔐 Настройка API ключа

Скопируйте файл `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и вставьте ваш API-ключ:

```
OPENAI_API_KEY=your_api_key_here
```

⚠️ Никогда не публикуйте файл `.env` в GitHub.

---

## ▶️ Запуск приложения

```bash
python -m app.main
```

Пример работы:

```
LLM CLI Assistant
Введите 'exit' для выхода

>> Объясни что такое нейронная сеть
```

---

## 📦 Зависимости

- openai
- python-dotenv
- rich

---


