# llm-cli-app


`llm-cli-app` — это Python CLI-инструмент для взаимодействия с крупными языковыми моделями (LLM).  
Он позволяет отправлять запросы к LLM, получать ответы, анализировать их, измерять токены и стоимость, а также сравнивать **контролируемый** и **обычный** режим генерации.

---


## 📦 Структура проекта



llm-cli-app/
├── app/
│ ├── init.py
│ ├── main.py # CLI и логика сравнения
│ ├── llm_client.py # Работа с OpenAI API, подсчет токенов и стоимости
│ └── comparator.py # Анализ и отображение сравнения ответов
├── requirements.txt
├── Readme.md
└── venv/


---

## ⚙️ Установка

Клонируйте репозиторий:

```bash
git clone https://github.com/AlekseyRodionov/llm_challenge.git
cd llm_challenge/llm-cli-app


Создайте виртуальное окружение и установите зависимости:

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

🔑 Настройка API

Для работы с OpenAI LLM необходимо задать переменные окружения:

export OPENAI_API_KEY="your_openai_api_key"
export OPENAI_BASE_URL="https://api.openai.com/v1"  # если используется кастомный URL
export OPENAI_MODEL="openai/gpt-4o-mini"            # по умолчанию


⚠️ Никогда не добавляйте ключи в репозиторий!

💻 Использование
Обычный запрос
python -m app.main "Расскажи о Python CLI инструментах"


Выводит:

Ответ LLM

Метрики: количество токенов, модель и стоимость

Контролируемый режим (с ограничениями)
python -m app.main "Расскажи о Python CLI инструментах" --compare


Выводит:

Ответ без ограничений

Ответ с контролируемыми ограничениями

И таблицу сравнения:

Метрика	Без ограничений	С ограничениями
Символы	256	123
Слова	45	30
Пункты (≈)	4	4
Завершение stop	True	True
⚡ Примеры

Быстрый вопрос:

python -m app.main "Напиши краткий обзор на тему LLM"


Сравнение с контролируемым ответом:

python -m app.main "Опиши преимущества Python" --compare

📊 Метрики и стоимость

llm_client.py подсчитывает:

input_tokens — токены входного запроса

output_tokens — токены ответа

total_tokens — суммарные токены

cost — примерная стоимость запроса на основе модели

Примерная стоимость моделей указана в MODEL_PRICES и рассчитывается пропорционально токенам.