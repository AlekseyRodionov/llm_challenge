# TempLab

CLI-инструмент для анализа влияния параметра `temperature` в LLM на качество ответа.

Проект выполняет один и тот же запрос с разными значениями `temperature` и сравнивает результаты по базовым эвристическим метрикам.

---

## 🎯 Цель

Исследовать влияние параметра:

* `temperature = 0`
* `temperature = 0.7`
* `temperature = 1.2`

Сравнение проводится по:

* точности (precision markers)
* креативности (creative markers)
* разнообразию (доля уникальных слов)

---

## 🏗 Структура проекта

```
├── app
│   ├── __init__.py
│   ├── comparator.py     # Анализ и сравнение ответов
│   ├── llm_client.py     # Работа с OpenAI API, токены, стоимость
│   └── main.py           # CLI и orchestration
├── README.md
├── requirements.txt
└── venv
```

---

## ⚙️ Установка

1. Создать виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # macOS / Linux
```

2. Установить зависимости:

```bash
pip install -r requirements.txt
```

3. Создать `.env` файл:

```
OPENAI_API_KEY=your_api_key
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=openai/gpt-4o-mini
```

---

## 🚀 Запуск

```bash
python main.py "Ваш запрос" --temp-experiment
```

Пример:

```bash
python main.py "Объясни что такое нейронная сеть" --temp-experiment
```

---

## 📊 Что делает программа

1. Выполняет запрос с тремя значениями temperature
2. Показывает спиннер во время ожидания ответа
3. Выводит:

   * текст ответа
   * токены (input / output / total)
   * примерную стоимость
4. Выполняет эвристический анализ
5. Строит сравнительную таблицу
6. Формирует итоговый вывод

---

## 🧠 Интерпретация результатов

| Temperature | Поведение                                          |
| ----------- | -------------------------------------------------- |
| 0           | Максимальная детерминированность, высокая точность |
| 0.7         | Баланс точности и креативности                     |
| 1.2         | Высокая вариативность, повышенная креативность     |

---

## 📦 Используемые библиотеки

* `openai`
* `tiktoken`
* `rich`
* `python-dotenv`

---
